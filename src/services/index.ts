import markdown from '@src/services/markdown'
import list from '@src/services/list'
import basicData from '@root/src/services/basicData'
import connection from '@root/src/services/connection'

export const services = {
  markdown,
  list,
  basicData,
  connection,
}
